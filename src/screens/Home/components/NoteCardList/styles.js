import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  emptyList: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },

  emptyListText: {
    fontSize: 20,
    fontWeight: 'bold',
  },
});
